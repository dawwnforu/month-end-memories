# 更新记录

本项目的正式版本与 GitHub Release 保持一致。

## release1.0.3

### 新增

- 根据原始名称补充疑似重复检查，忽略导出时间与复制编号，显示缩略图和定位入口，核对后可清理每组多余图片；
- 左侧图片详情上方新增名称搜索，支持多结果列表、跨页定位、高亮及 Enter / Shift+Enter 切换；
- 重复图片悬浮面板显示图片名称、图片序号、页码、页内位置与打印副本位置；
- 一键清理多余图片，每组保留第一张及其编辑设置和打印份数，可撤销恢复。

### 修复

- 修复删除重复组中的图片后判重记录失效，导致同图可以再次导入的问题；
- 判重改为比较解码后的完整像素和尺寸，可识别改名或仅元数据不同的同图，同名不同图正常保留；
- 批量导入依次解码，避免同时分配全部图片的像素缓冲区。

重新压缩、缩放或修改画面后的相似图片不会自动合并。

### 改进

- 页面固定文字禁用选择、复制与剪切，并隐藏非输入区域的文本光标；
- 照片与品牌图片禁用拖拽，照片区域阻止右键复制；
- 数字输入框和选择器继续保留正常编辑能力；
- 明确照片只在当前浏览器本地处理，界面保护用于减少误操作。

## release1.0.2

发布日期：2026-07-29

本版本完善了照片导入、自由编辑、纸张规格与操作效率。

### 新增

- 导入时按图片内容检测重复项，自动跳过后续重复照片；
- 重复图片弹窗会显示该图片在已有列表与本次选择中的实际总数；
- 新增“是否允许图片重复”开关；开启后相同图片可分别导入，且不会显示重复提醒；
- 新增 A3、A5、A6、B5、Letter、5 寸和 6 寸纸张规格；
- 新增纸张真实大小参考图，可切换所有可用纸张及多种常见参照物，并查看同比例宽高换算；
- 新增右上角问题反馈入口与 GitHub 结构化反馈表单；
- 选中照片后可使用 `Delete` / `Backspace` 一键删除；
- 使用 `+` / `-` 以 1 mm 步长缓慢放大或缩小照片；
- 使用 `R` 顺时针旋转选中照片，使用 `Esc` 取消选择；
- 使用 `Ctrl + Z` / `⌘ + Z` 撤销删除、缩放、旋转和排版设置；
- 新增可点击的撤销按钮与小问号快捷键下拉提示；
- 输入宽、高、份数、边距和缝隙时自动停用页面快捷键，避免误操作；
- 新增可移动、可拖动四角的自由裁剪框，裁剪结果同步用于预览和 PDF；
- 每张照片可解除宽高比例锁定，分别输入或拖动调整宽度和高度。

## release1.0.1

发布日期：2026-07-28

这是「月末拾光」首个正式公开版本。

### 新增

- 多张照片批量导入；
- 按毫米设置照片打印尺寸和份数；
- A4 纵向、横向排版；
- 自定义页边距和照片间隙；
- 自动旋转照片并计算省纸排列；
- 单张照片顺时针 90° 手动旋转；
- 预览区四角拖动缩放；
- 缩放过程中的实时重新排版和分页更新；
- 当前编辑照片的蓝色高亮与尺寸提示；
- 300 DPI 多页 A4 PDF 导出；
- 月末拾光品牌 Logo、分享图片和公开在线站点。

### 隐私

- 照片只在当前浏览器中读取和处理；
- 不需要登录；
- 不使用数据库保存照片；
- 刷新或关闭页面后清除本次排版状态。

### 打印提醒

- 打印纸张选择 A4；
- 缩放选择“实际大小”或“100%”；
- 不要选择“适合页面”。
